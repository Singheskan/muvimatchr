services:
  db:
    image: postgres:18
    environment:
      POSTGRES_DB: muvimatchr
      POSTGRES_USER: muvimatchr
      POSTGRES_PASSWORD: muvimatchr
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - muvimatchr-pgdata:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U muvimatchr -d muvimatchr"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  muvimatchr-pgdata:
