---
phase: 02-session-lobby-flow
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - build.gradle.kts
  - src/main/kotlin/org/example/muvimatchr/session/ParticipantController.kt
  - src/test/kotlin/org/example/muvimatchr/session/ParticipantControllerTest.kt
  - src/test/kotlin/org/example/muvimatchr/session/SessionServiceTest.kt
autonomous: true
requirements: [SESH-01, SESH-02, SESH-04]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "POST /api/sessions/{joinCode}/participants with a blank or 101+ character displayName returns 400 and never persists a Participant row."
    - "spring-boot-starter-validation is present on the compile and runtime classpath (not merely declared in build.gradle.kts) so @NotBlank/@Size are actually enforced, not silently ignored."
    - "Three or more distinct participants can join the same session, each receiving a distinct participantId and a distinct token; the participant table has exactly that many rows for that session."
    - "Two independent calls to SessionService.createSession() never return the same joinCode."
  artifacts:
    - path: "build.gradle.kts"
      provides: "Jakarta Bean Validation (Hibernate Validator) on the classpath"
      contains: "spring-boot-starter-validation"
    - path: "src/main/kotlin/org/example/muvimatchr/session/ParticipantController.kt"
      provides: "validated JoinRequest, rejecting blank/oversized display names with 400"
      contains: "NotBlank"
    - path: "src/test/kotlin/org/example/muvimatchr/session/ParticipantControllerTest.kt"
      provides: "validation, multi-participant, and full resume-flow proof added to Plan 02-01's suite"
      contains: "displayName"
    - path: "src/test/kotlin/org/example/muvimatchr/session/SessionServiceTest.kt"
      provides: "join-code distinctness/shape proof for SESH-01"
      contains: "createSession"
  key_links:
    - from: "build.gradle.kts"
      to: "src/main/kotlin/org/example/muvimatchr/session/ParticipantController.kt"
      via: "spring-boot-starter-validation supplies the jakarta.validation.constraints annotations used on JoinRequest.displayName"
      pattern: "jakarta.validation"
    - from: "src/main/kotlin/org/example/muvimatchr/session/ParticipantController.kt"
      to: "src/test/kotlin/org/example/muvimatchr/session/ParticipantControllerTest.kt"
      via: "the @Valid-triggered 400 response the new tests assert against"
      pattern: "@Valid"
---

<objective>
Close out the phase's remaining requirement coverage on top of Plan 02-01's proven create/join/resolve/resume path: add real Bean Validation on the join request (resolving the exact gap the retry history for this phase flagged — `spring-boot-starter-validation` is NOT transitively pulled in by `spring-boot-starter-web` since Spring Boot 2.3, confirmed absent from this project's `runtimeClasspath` this session), and add the two proof tests 02-VALIDATION.md's Wave 0 gap list still requires: multi-participant support (SESH-04) and join-code distinctness (SESH-01).

Purpose: Plan 02-01 proved the architecture works for one participant on one path; this plan refines that proven slice with input validation and widens the proof to the group-size and uniqueness guarantees the phase's requirements actually promise (2+ participants, never-duplicate join codes) — it adds no new architecture, only hardening and evidence on top of what Plan 02-01 already built.

Output: `displayName` is genuinely validated (not merely typed as `String`), and `SessionServiceTest.kt` + expanded `ParticipantControllerTest.kt` close every remaining Wave 0 gap in 02-VALIDATION.md.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-session-lobby-flow/02-CONTEXT.md
@.planning/phases/02-session-lobby-flow/02-RESEARCH.md
@.planning/phases/02-session-lobby-flow/02-PATTERNS.md
@.planning/phases/02-session-lobby-flow/02-VALIDATION.md
@.planning/phases/02-session-lobby-flow/02-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Real Bean Validation on the join request</name>
  <precondition>Plan 02-01 is complete: `ParticipantController.kt` exists with a `JoinRequest(val displayName: String)` data class and no `jakarta.validation` import (Plan 02-01's Task 1 deliberately left it unvalidated).</precondition>
  <files>build.gradle.kts, src/main/kotlin/org/example/muvimatchr/session/ParticipantController.kt, src/test/kotlin/org/example/muvimatchr/session/ParticipantControllerTest.kt</files>
  <read_first>
    - `build.gradle.kts` — current dependency block; confirm `spring-boot-starter-validation` is genuinely absent before adding it (this project's own retry history for this phase independently confirmed via `./gradlew dependencies --configuration runtimeClasspath` that no validation artifact resolves transitively from `spring-boot-starter-web`).
    - `.planning/phases/02-session-lobby-flow/02-RESEARCH.md` — Don't Hand-Roll table (Bean Validation row) and Security Domain V5 Input Validation row — both assumed the starter was already present; it is not, so this task supplies it explicitly.
    - `src/main/kotlin/org/example/muvimatchr/session/ParticipantController.kt` — the file as Plan 02-01 left it (both endpoints present, `JoinRequest` unvalidated).
    - `src/test/kotlin/org/example/muvimatchr/session/Participant.kt` line with `@Column(name = "display_name", nullable = false, length = 100)` — the DB column width the validation must match.
  </read_first>
  <behavior>
    - POST join with `{"displayName":""}` returns 400.
    - POST join with a 101-character `displayName` returns 400.
    - POST join with `{"displayName":"Alice"}` still returns 201 (no regression from adding validation).
  </behavior>
  <action>
    Add `implementation("org.springframework.boot:spring-boot-starter-validation")` to `build.gradle.kts`, placed alongside the other `implementation(...)` starter lines (near `spring-boot-starter-web`), with a trailing `//` comment matching this file's existing comment style noting it supplies Jakarta Bean Validation (Hibernate Validator) and is required explicitly because it is not transitively pulled in by `spring-boot-starter-web` since Spring Boot 2.3.

    In `ParticipantController.kt`, import `jakarta.validation.Valid`, `jakarta.validation.constraints.NotBlank`, and `jakarta.validation.constraints.Size`. Annotate `JoinRequest.displayName` with `@field:NotBlank` and `@field:Size(max = 100)` (matching the `Participant.displayName` column's `length = 100`). Add `@Valid` in front of the `@RequestBody request: JoinRequest` parameter on the `join` method. No custom exception handler is needed — Spring Boot's default `MethodArgumentNotValidException` handling already returns 400 for a failed `@Valid`.

    Extend `ParticipantControllerTest.kt` with the three behaviors above. Additionally assert that the blank-name and oversized-name requests do not create a participant row: capture `participantRepository.count()` before each invalid POST and assert it is unchanged after.
  </action>
  <acceptance_criteria>
    - `build.gradle.kts` contains the literal `spring-boot-starter-validation`.
    - `ParticipantController.kt` contains `@field:NotBlank` on `JoinRequest.displayName` and `@Valid` on the controller parameter.
    - `./gradlew dependencies --configuration compileClasspath` output contains `hibernate-validator` (proves the transitive implementation actually resolved onto the classpath, not just the starter coordinate being declared).
    - `./gradlew test --tests "*.ParticipantControllerTest"` exits 0 and reports 11 executed tests (8 from Plan 02-01 plus 3 from this task), not fewer.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'spring-boot-starter-validation' build.gradle.kts</automated>
    <fails_when>0 — the dependency line is missing from build.gradle.kts</fails_when>
    <automated>./gradlew dependencies --configuration compileClasspath | grep -q 'hibernate-validator'</automated>
    <fails_when>non-zero exit — the validation starter's Bean Validation implementation did not actually resolve onto the compile classpath, which means @NotBlank/@Size would be silently unenforced at runtime</fails_when>
    <automated>./gradlew test --tests "*.ParticipantControllerTest"</automated>
    <fails_when>non-zero exit, or "BUILD FAILED" in the output, or fewer than 11 tests reported executed</fails_when>
  </verify>
  <done>A blank or oversized display name is rejected with 400 before it reaches persistence, and the fix is proven by the actual Hibernate Validator implementation resolving on the classpath, not just the annotation compiling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Multi-participant support and join-code distinctness proofs</name>
  <precondition>Plan 02-01's `ParticipantControllerTest.kt` already exercises join and resume via MockMvc against real Postgres; this task extends that same file plus a new `SessionServiceTest.kt`, both layered on `PostgresTestSupport`.</precondition>
  <files>src/test/kotlin/org/example/muvimatchr/session/ParticipantControllerTest.kt, src/test/kotlin/org/example/muvimatchr/session/SessionServiceTest.kt</files>
  <read_first>
    - `.planning/phases/02-session-lobby-flow/02-PATTERNS.md` — `session/SessionServiceTest.kt` section (`@Autowired lateinit var sessionService: SessionService`, asserts distinctness across two rapid calls, no need to force a real DB collision).
    - `src/test/kotlin/org/example/muvimatchr/voting/VoteRepositoryTest.kt` — the `JdbcTemplate`-based row-count assertion style (`voteCount` helper) to mirror for a per-session participant-count assertion.
    - `src/main/kotlin/org/example/muvimatchr/session/SessionService.kt` (written in Plan 02-01) — the `createSession()` signature and the `JOIN_CODE_LENGTH`/`JOIN_CODE_ALPHABET` constants this test's length assertion checks against.
    - `src/test/kotlin/org/example/muvimatchr/session/SessionRepositoryTest.kt` — base-class/style template (`PostgresTestSupport`, `@Autowired lateinit var`, JUnit 5 `Assertions`).
  </read_first>
  <behavior>
    - A session joined by three participants ("Alice", "Bob", "Carol") ends up with exactly 3 distinct `participantId`s, 3 distinct `token`s, and exactly 3 rows in `participant` for that `session_id`.
    - Two back-to-back `SessionService.createSession()` calls never return the same `joinCode`.
    - Each returned `joinCode` is non-blank, exactly `JOIN_CODE_LENGTH` (6) characters long, and fits within the `VARCHAR(16)` column.
  </behavior>
  <action>
    Extend `ParticipantControllerTest.kt` with a test that creates one session, then issues three sequential `POST /api/sessions/{joinCode}/participants` calls with display names `"Alice"`, `"Bob"`, `"Carol"`. Assert all three responses are 201, collect the three `participantId` values into a set and assert its size is 3, collect the three `token` values into a set and assert its size is 3 (SESH-04: no hardcoded 2-participant cap). Using the autowired `JdbcTemplate` (mirror `VoteRepositoryTest.kt`'s helper-method style), run `SELECT count(*) FROM participant WHERE session_id = ?` for that session's id and assert the result is exactly 3.

    Create `src/test/kotlin/org/example/muvimatchr/session/SessionServiceTest.kt`, extending `PostgresTestSupport`, autowiring `SessionService`. Test 1: call `createSession()` twice, assert the two `joinCode` values are not equal. Test 2: call `createSession()` once, assert `joinCode.isNotBlank()`, `joinCode.length == 6`, and `joinCode.length <= 16`.
  </action>
  <acceptance_criteria>
    - `ParticipantControllerTest.kt` contains a test asserting 3 distinct participant ids and 3 distinct tokens for one session.
    - `SessionServiceTest.kt` exists, extends `PostgresTestSupport`, and contains `createSession`.
    - `./gradlew test --tests "*.ParticipantControllerTest"` exits 0 and reports 12 executed tests (11 from Task 1 plus 1 from this task), not fewer.
    - `./gradlew test --tests "*.SessionServiceTest"` exits 0 and reports 2 executed tests.
    - `./gradlew test` (full suite: all Phase 1 tests plus all of this phase's tests) exits 0.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*.ParticipantControllerTest"</automated>
    <fails_when>non-zero exit, or "BUILD FAILED" in the output, or fewer than 12 tests reported executed</fails_when>
    <automated>./gradlew test --tests "*.SessionServiceTest"</automated>
    <fails_when>non-zero exit, or "BUILD FAILED" in the output, or "No tests found for given includes" in the output</fails_when>
    <automated>./gradlew test</automated>
    <fails_when>non-zero exit, or "BUILD FAILED" in the output, or "FAILED" reported for any test class</fails_when>
  </verify>
  <done>A session accepts 3+ distinct participants with no cap, join-code generation is proven non-colliding across repeated calls, and the full project test suite (Phase 1 + Phase 2) is green together.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> POST /api/sessions/{joinCode}/participants (displayName field) | Untrusted free-text input now validated at the DTO boundary before it reaches the `display_name VARCHAR(100)` column |
| Build system -> Gradle dependency resolution | A new first-party dependency coordinate is added this plan; not a runtime request-facing boundary, but still a supply-chain surface worth naming |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Tampering | `JoinRequest.displayName` (oversized/blank input into a fixed-width column) | medium | mitigate | `jakarta.validation` `@field:NotBlank @field:Size(max = 100)` on `JoinRequest.displayName` plus `@Valid` on the controller parameter, matching the DB column width exactly — rejected with 400 before reaching persistence, verified via a classpath check that Hibernate Validator actually resolved, not just that the annotation compiled |
| T-02-SC | Tampering | Gradle dependency add: `org.springframework.boot:spring-boot-starter-validation` | low | mitigate | Official first-party Spring Boot starter, version-managed by the same Spring Boot 4.1.1 BOM already governing `spring-boot-starter-web`/`spring-boot-starter-data-jpa`/`spring-boot-starter-websocket`/`spring-boot-starter-thymeleaf` already present in `build.gradle.kts` — not a third-party npm/pip/cargo package, so the Package Legitimacy Gate (scoped to npm/pip/cargo) does not apply; the coordinate is verified directly against the already-present, already-trusted Spring Boot dependency-management plugin rather than an ad hoc registry lookup |

</threat_model>

<verification>
Run `./gradlew test --tests "*.ParticipantControllerTest"` and `./gradlew test --tests "*.SessionServiceTest"` after each task, then `./gradlew test` (full suite) at the end of Task 2. All 02-VALIDATION.md Wave 0 gaps (`SessionServiceTest.kt`, the multi-participant/validation additions to `ParticipantControllerTest.kt`) are closed by the end of this plan.
</verification>

<success_criteria>
- A single session can be joined by 3+ distinct participants, confirming group support isn't hardcoded to exactly 2 (ROADMAP Phase 2 success criterion 3).
- Session join codes are proven distinct across repeated creation (supports ROADMAP success criterion 1 alongside Plan 02-01's DB-level unique index).
- Malformed input (blank/oversized display name) is rejected before persistence, closing 02-RESEARCH.md's Security Domain V5 gap.
</success_criteria>

## Artifacts this phase produces

Symbols/paths this plan adds on top of Plan 02-01:

- `build.gradle.kts` — new dependency: `org.springframework.boot:spring-boot-starter-validation`
- `ParticipantController.JoinRequest.displayName` — now annotated `@field:NotBlank @field:Size(max = 100)`
- `ParticipantController.join()` — `@Valid` added to the `JoinRequest` parameter
- `src/test/kotlin/org/example/muvimatchr/session/SessionServiceTest.kt` — new test file, 2 test methods
- `src/test/kotlin/org/example/muvimatchr/session/ParticipantControllerTest.kt` — grows to 12 test methods total across both plans

This completes all five phase requirements (SESH-01 through SESH-05) and all four ROADMAP Phase 2 success criteria's participant-identity guarantees (vote-attachment proof for success criterion 4 is deferred to Phase 4, once votes exist to attach).

<output>
Create `.planning/phases/02-session-lobby-flow/02-02-SUMMARY.md` when done
</output>
