---
phase: 01-persistence-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build.gradle.kts
  - gradle/wrapper/gradle-wrapper.properties
  - src/main/resources/application.properties
  - docker-compose.yml
  - src/test/kotlin/org/example/muvimatchr/MuviMatchrApplicationTests.kt
autonomous: false
requirements: [RELI-01]

user_setup:
  - service: container-runtime
    why: "Testcontainers has no non-Docker execution mode. Every integration test in this phase (and the restart-survival proof that IS this phase's deliverable) needs a Docker-API-compatible daemon. None is installed on this machine — re-verified this planning session: `command -v docker`, `colima`, `podman`, `orbstack`, `nerdctl` are all empty and /Applications holds no container app. This confirms RESEARCH.md Assumption A2 (it is a genuine absence, not a PATH problem)."
    dashboard_config:
      - task: "Approve which container runtime to install (Task 1 checkpoint:decision). Claude then installs and starts it via Homebrew in Task 2 — no manual install steps for you."
        location: "Local machine. Homebrew is already present at /opt/homebrew/bin/brew (verified this session, v6.0.14), so the CLI-installable options need no GUI or license click-through."

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`./gradlew --version` reports Gradle 8.14.3 — at or above the 8.14 floor the Spring Boot 4.1 Gradle plugin requires."
    - "`./gradlew build` succeeds on Spring Boot 4.1.1 + Kotlin 2.3.20 with the persistence dependency set on the classpath, and no pre-existing Thymeleaf/web/WebSocket dependency was removed."
    - "A Docker-API-compatible daemon is installed and running: `docker info` exits 0 and `docker compose version` exits 0."
    - "`docker compose up -d` starts a PostgreSQL 18 service that reports ready, bound to loopback only."
    - "Hibernate never writes DDL: `spring.jpa.hibernate.ddl-auto` is set to `validate` and appears exactly once in application.properties."
  artifacts:
    - path: "build.gradle.kts"
      provides: "Boot 4.1.1 / Kotlin 2.3.20 toolchain plus the JPA + Flyway + Postgres + Testcontainers dependency set"
      contains: "spring-boot-starter-flyway"
    - path: "gradle/wrapper/gradle-wrapper.properties"
      provides: "Gradle 8.14.3 wrapper distribution"
      contains: "gradle-8.14.3-bin.zip"
    - path: "docker-compose.yml"
      provides: "Local PostgreSQL 18 dev database matching the Testcontainers image tag"
      contains: "postgres:18"
    - path: "src/main/resources/application.properties"
      provides: "Datasource, Flyway, and ddl-auto=validate configuration for local bootRun"
      contains: "spring.jpa.hibernate.ddl-auto=validate"
  key_links:
    - from: "build.gradle.kts"
      to: "Flyway auto-configuration at application startup"
      via: "the dedicated Boot 4 Flyway starter — the bare core artifact used by every pre-4.0 tutorial compiles but never auto-configures (RESEARCH.md Pitfall 2)"
      pattern: "spring-boot-starter-flyway"
    - from: "docker-compose.yml"
      to: "src/main/resources/application.properties"
      via: "POSTGRES_DB / POSTGRES_USER / POSTGRES_PASSWORD must equal the database name, username and password in the JDBC datasource properties, or local bootRun cannot connect"
      pattern: "muvimatchr"
  prohibitions:
    - requirement_id: RELI-01
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT let the restart-survival proof report green without actually exercising a real PostgreSQL instance and two genuinely separate application contexts — no pass that comes from tests being skipped or aborted because no container daemon was found, and no embedded or in-memory database substituted for PostgreSQL. A green suite that proves zero durability is the exact failure this phase exists to eliminate."
---

<objective>
Make this repository capable of persistence work at all: raise the build toolchain to the versions D-01 locks (Spring Boot 4.1.1, Kotlin 2.3.20, Gradle wrapper 8.14.3), add the JPA + Flyway + PostgreSQL + Testcontainers dependency set, install and start a container runtime, and stand up the local PostgreSQL service D-02 requires.

Purpose: this plan is the enabling prerequisite for the phase, not an application layer. It contains no domain behavior by design — under tracer-first decomposition the tracer slice must be able to compile and run, and right now neither is possible: the Gradle wrapper (8.10) is below the floor the Boot 4.1 plugin requires, there is no JPA/Flyway/Postgres driver on the classpath, and there is no container daemon on this machine for Testcontainers to talk to. The end-to-end tracer is Plan 01-02, which runs immediately after this one.

Output: a build that compiles on the D-01 toolchain with persistence dependencies resolved, a running container daemon, a local PostgreSQL 18 service, and datasource/Flyway configuration wired for `bootRun`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-persistence-foundation/01-CONTEXT.md
@.planning/phases/01-persistence-foundation/01-RESEARCH.md
@.planning/phases/01-persistence-foundation/01-PATTERNS.md
@.planning/phases/01-persistence-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Choose the container runtime to install (unblocks D-02 and D-03)</name>
  <decision>Which Docker-API-compatible container runtime should be installed on this development machine?</decision>
  <context>
    Testcontainers has no non-Docker execution mode, so this phase's entire proof of RELI-01 — and every integration test in every later phase — is blocked until a daemon exists. Nothing is installed today (verified this session: no `docker`, `colima`, `podman`, `orbstack` or `nerdctl` binary, no container app in /Applications). Homebrew IS present at /opt/homebrew/bin/brew, so the CLI-installable options need no GUI or license click-through and Claude can install and start the chosen one for you in the very next task.

    This is a decision rather than something Claude just does, because it installs a persistent Linux VM on your machine and the licensing terms differ materially between the options. It is fully reversible either way — Testcontainers only needs a Docker socket, so swapping runtimes later is a `brew uninstall` plus a `brew install`, with no impact on any code, schema, or contract in this project.
  </decision>
  <options>
    <option id="colima">
      <name>Colima (recommended)</name>
      <pros>Apache-2.0, free for any use including commercial. Pure CLI — `brew install colima docker docker-compose` then `colima start`, no GUI and no license acceptance step, so Claude can complete the whole install unattended. Lets you cap the VM's CPU/RAM explicitly. Testcontainers detects its socket automatically.</pros>
      <cons>No GUI dashboard for inspecting containers. VM must be restarted with `colima start` after a machine reboot (not automatic by default). Slightly slower filesystem mounts than OrbStack — irrelevant for a Postgres container.</cons>
    </option>
    <option id="docker-desktop">
      <name>Docker Desktop</name>
      <pros>The reference implementation; every Testcontainers document assumes it. GUI dashboard. Starts automatically at login.</pros>
      <cons>Installs as a cask with a GUI first-run flow and a licence acceptance step, so the install cannot be fully completed unattended. Requires a paid subscription for companies over Docker's size threshold. Heaviest resource footprint of the three.</cons>
    </option>
    <option id="orbstack">
      <name>OrbStack</name>
      <pros>Fastest and lightest of the three on Apple Silicon; drop-in Docker CLI compatibility; good GUI.</pros>
      <cons>Free for personal use only — commercial use requires a paid licence. Cask install with a GUI first-run flow, so again not fully unattended.</cons>
    </option>
  </options>
  <resume-signal>Select: colima, docker-desktop, or orbstack</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install the chosen container runtime and upgrade the build toolchain to Boot 4.1.1 / Kotlin 2.3.20 / Gradle 8.14.3</name>
  <reversibility rating="costly">Carried forward from D-01, which the developer rated `costly`: once Phases 2-6 write code against Boot 4 / Kotlin 2.3-era APIs (JSpecify nullability, Jakarta EE 11, Jackson 3 defaults), reverting means re-touching every phase built on top, not just this one.</reversibility>
  <files>gradle/wrapper/gradle-wrapper.properties, build.gradle.kts, src/test/kotlin/org/example/muvimatchr/MuviMatchrApplicationTests.kt</files>
  <read_first>
    - `build.gradle.kts` — the file being modified. Current state: `kotlin("jvm") 1.9.25`, `kotlin("plugin.spring") 1.9.25`, `org.springframework.boot 3.3.3`, `io.spring.dependency-management 1.1.6`; dependencies are thymeleaf, web, jackson-module-kotlin, kotlin-reflect, devtools, spring-boot-starter-test, kotlin-test-junit5, junit-platform-launcher, websocket; plus a `kotlin { compilerOptions { freeCompilerArgs.addAll("-Xjsr305=strict") } }` block and `tasks.withType<Test> { useJUnitPlatform() }`.
    - `gradle/wrapper/gradle-wrapper.properties` — the file being modified. Line 4 currently pins the 8.10 distribution.
    - `src/test/kotlin/org/example/muvimatchr/MuviMatchrApplicationTests.kt` — the file being deleted. Confirm before deleting that it is still a bare `@SpringBootTest` with a single empty `contextLoads()` body and no assertions.
    - `.planning/phases/01-persistence-foundation/01-RESEARCH.md` — the Standard Stack table (every coordinate is BOM-verified; do not substitute coordinates recalled from tutorials), Pitfall 1 (Testcontainers 2.0 artifact-id rename), Pitfall 2 (Boot 4 needs the dedicated Flyway starter), Pitfall 3 (JSpecify nullability compile errors).
    - `.planning/phases/01-persistence-foundation/01-PATTERNS.md` — the `build.gradle.kts` and `gradle-wrapper.properties` sections, which state the exact current-to-target diffs and the rule that no existing dependency may be removed.
  </read_first>
  <action>
    First install the runtime the developer selected in Task 1. For `colima`, run `brew install colima docker docker-compose` then `colima start`. For `docker-desktop`, run `brew install --cask docker`; for `orbstack`, run `brew install --cask orbstack` — then open the app once so its first-run flow completes and the socket appears. If any brew command fails on permissions or authentication, do not retry in a loop: raise a `checkpoint:human-action` describing the exact failing command and its error, then resume. Confirm the socket is live with `docker info` and `docker compose version` before touching the build files.

    Then bump the wrapper: in `gradle/wrapper/gradle-wrapper.properties`, change only the `distributionUrl` value to the Gradle 8.14.3 binary distribution at services.gradle.org — `distributionUrl=https\://services.gradle.org/distributions/gradle-8.14.3-bin.zip`. Leave `distributionBase`, `distributionPath`, `networkTimeout`, `validateDistributionUrl`, `zipStoreBase` and `zipStorePath` untouched. 8.14.3 is required, not cosmetic: the Boot 4.1 Gradle plugin refuses anything below 8.14, and 8.14.3 stays inside Kotlin 2.3.20's tested 7.6.3-9.3.0 range.

    Then update `build.gradle.kts` per D-01. In the plugins block set `kotlin("jvm")` and `kotlin("plugin.spring")` to version `2.3.20`, add `kotlin("plugin.jpa") version "2.3.20"`, set `id("org.springframework.boot")` to `4.1.1`, and set `id("io.spring.dependency-management")` to `1.1.7`. Do NOT add a manual `allOpen { ... }` block: as of Kotlin 2.3.20 the JPA plugin applies the all-open compiler plugin with a built-in JPA preset on its own, so the block older guides show is redundant.

    In the dependencies block, ADD (never replace, never reorder, never remove) seven coordinates, all unversioned because `io.spring.dependency-management` imports the Boot 4.1.1 BOM: `implementation("org.springframework.boot:spring-boot-starter-data-jpa")`, `implementation("org.springframework.boot:spring-boot-starter-flyway")`, `implementation("org.flywaydb:flyway-database-postgresql")`, `implementation("org.postgresql:postgresql")`, `testImplementation("org.springframework.boot:spring-boot-testcontainers")`, `testImplementation("org.testcontainers:testcontainers-postgresql")`, `testImplementation("org.testcontainers:testcontainers-junit-jupiter")`. Two of these are easy to get wrong from memory: Boot 4 moved Flyway auto-configuration into its own starter, so the dedicated starter above is mandatory and the bare core artifact every pre-4.0 tutorial shows will compile while silently never running a migration; and Testcontainers 2.0 renamed its modules with a `testcontainers-` artifact-id prefix, so the un-prefixed 1.x artifact ids will resolve an unmanaged version against Boot 4.1.1's BOM-managed Testcontainers 2.0.5.

    Leave the `java { toolchain { languageVersion = JavaLanguageVersion.of(21) } }` block at 21 — a temurin-21 JDK is installed on this machine and Boot 4.1's floor is 17, so no change is needed. Leave `-Xjsr305=strict` in place. If the Kotlin compiler now reports nullability errors on types from Spring Data or Jakarta Persistence packages, that is the expected JSpecify strictness Boot 4 introduces (RESEARCH.md Pitfall 3): fix the Kotlin declaration's nullability to match, and do not relax or remove the compiler flag to make it go away.

    Finally, delete `src/test/kotlin/org/example/muvimatchr/MuviMatchrApplicationTests.kt`. With JPA on the classpath, a bare `@SpringBootTest` needs a reachable datasource, so this placeholder would fail at context startup with no way to satisfy it until the Testcontainers fixture exists. It asserts nothing today — a single empty `contextLoads()` body — and Plan 01-02's `RestartSurvivalTest` supersedes it strictly: that test starts the real `MuviMatchrApplication` context twice against a real PostgreSQL instance, so context-startup coverage goes up, not down. Do not leave it disabled or annotated-out; delete it, and record the supersession in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `docker info` exits 0 and its output contains a `Server Version:` line.
    - `docker compose version` exits 0.
    - `./gradlew --version` output contains `Gradle 8.14.3`.
    - `build.gradle.kts` contains all five plugin lines at their target versions: `kotlin("jvm") version "2.3.20"`, `kotlin("plugin.spring") version "2.3.20"`, `kotlin("plugin.jpa") version "2.3.20"`, `id("org.springframework.boot") version "4.1.1"`, `id("io.spring.dependency-management") version "1.1.7"`.
    - `build.gradle.kts` contains all seven new coordinates listed in the action, including the literal `spring-boot-starter-flyway` and the literal `org.testcontainers:testcontainers-postgresql`.
    - `build.gradle.kts` still contains every pre-existing dependency: `spring-boot-starter-thymeleaf`, `spring-boot-starter-web`, `jackson-module-kotlin`, `kotlin-reflect`, `spring-boot-devtools`, `spring-boot-starter-test`, `kotlin-test-junit5`, `junit-platform-launcher`, `spring-boot-starter-websocket`.
    - `./gradlew dependencies --configuration runtimeClasspath` resolves without error and its output shows `org.postgresql:postgresql` and `org.flywaydb:flyway-database-postgresql`.
    - `src/test/kotlin/org/example/muvimatchr/MuviMatchrApplicationTests.kt` no longer exists.
    - `./gradlew build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>docker info > /dev/null && docker compose version > /dev/null && echo RUNTIME_OK</automated>
    <fails_when>non-zero exit, `Cannot connect to the Docker daemon` in the output, or `RUNTIME_OK` is absent from the output</fails_when>
    <automated>./gradlew --version | grep -q 'Gradle 8\.14\.3'</automated>
    <fails_when>non-zero exit — the wrapper still resolves a version other than 8.14.3, or the wrapper distribution failed to download</fails_when>
    <automated>./gradlew build</automated>
    <fails_when>non-zero exit, or `BUILD FAILED` / `Could not resolve` / `e: file://` (Kotlin compile error) in the output</fails_when>
    <automated>./gradlew dependencies --configuration runtimeClasspath | grep -q 'org.flywaydb:flyway-database-postgresql'</automated>
    <fails_when>non-zero exit — the Flyway PostgreSQL dialect module is not on the runtime classpath, which means Flyway would fail at first migration against Postgres</fails_when>
    <automated>test ! -e src/test/kotlin/org/example/muvimatchr/MuviMatchrApplicationTests.kt</automated>
    <fails_when>non-zero exit — the superseded placeholder test still exists and will fail at context startup for want of a datasource</fails_when>
  </verify>
  <done>A container daemon is running; `./gradlew --version` reports 8.14.3; `./gradlew build` is green on Boot 4.1.1 + Kotlin 2.3.20 with all seven persistence/Testcontainers coordinates resolved and every pre-existing dependency retained; the superseded placeholder test is deleted.</done>
</task>

<task type="auto">
  <name>Task 3: Add the local PostgreSQL 18 compose service and wire datasource, Flyway and ddl-auto=validate configuration</name>
  <reversibility rating="reversible">Carried forward from D-02, which the developer rated `reversible`: a compose file and five properties lines can be added, changed or removed with no schema or contract impact.</reversibility>
  <files>docker-compose.yml, src/main/resources/application.properties</files>
  <read_first>
    - `src/main/resources/application.properties` — the file being modified. It currently holds exactly one line: `spring.application.name=MuviMatchr`. That line must survive.
    - `.planning/phases/01-persistence-foundation/01-PATTERNS.md` — the `application.properties` and `docker-compose.yml` sections, which give the target additive property block and the compose-file constraints.
    - `.planning/phases/01-persistence-foundation/01-RESEARCH.md` — Pitfall 4 (the PostgreSQL 18 image's data-directory path change) and Open Question 2 (why the image tag is 18 and what it costs to change later).
    - `.planning/research/STACK.md` — the "What NOT to Use" table, which forbids Hibernate schema generation outright and makes Flyway the only writer of DDL.
  </read_first>
  <action>
    Create `docker-compose.yml` at the repository root per D-02, with a single service named `db`. Set `image: postgres:18` — this must equal the tag the Testcontainers fixture uses in Plan 01-02, so the local database and the test database share a dialect. Set environment `POSTGRES_DB=muvimatchr`, `POSTGRES_USER=muvimatchr`, `POSTGRES_PASSWORD=muvimatchr`. Publish the port as `127.0.0.1:5432:5432`, not the bare `5432:5432` form: the bare form binds every interface, which would put a Postgres instance holding trivial well-known credentials on whatever network this laptop is attached to. Declare a named volume `muvimatchr-pgdata` and mount it at `/var/lib/postgresql` — NOT at the `/data` suffix path older guides use, because the PostgreSQL 18 image relocated its internal data directory and a mount at the old path silently produces an empty volume whose contents vanish on `docker compose down`. Add a healthcheck running `pg_isready -U muvimatchr -d muvimatchr` so `docker compose up --wait` can block until the database actually accepts connections.

    Then append to `src/main/resources/application.properties`, keeping the existing `spring.application.name=MuviMatchr` line first: `spring.datasource.url=jdbc:postgresql://localhost:5432/muvimatchr`, `spring.datasource.username=muvimatchr`, `spring.datasource.password=muvimatchr`, `spring.jpa.hibernate.ddl-auto=validate`, and `spring.flyway.enabled=true`. The three datasource values must match the compose service's environment exactly or local `bootRun` cannot connect. Write `spring.jpa.hibernate.ddl-auto` exactly once and set it to `validate` — Flyway owns every DDL statement in this project and Hibernate is only ever allowed to check that the mapped entities agree with the schema Flyway built. The generate-and-mutate settings for that property are forbidden by `.planning/research/STACK.md`; they drift the schema with no version history, which is one of the two documented root causes this whole phase exists to make structurally impossible.

    Finally bring the service up with `docker compose up -d --wait` and confirm it reports healthy. Leave it running — Plan 01-02 and 01-03 do not depend on it (they use Testcontainers), but D-02's stated purpose is that a local database exists to explore against.
  </action>
  <acceptance_criteria>
    - `docker compose config` exits 0, proving the compose file parses.
    - `docker-compose.yml` contains the literal `postgres:18` and the literal `127.0.0.1:5432:5432`.
    - `docker-compose.yml` mounts the named volume at `/var/lib/postgresql`.
    - `docker compose ps` shows the `db` service with a healthy status.
    - `src/main/resources/application.properties` still contains `spring.application.name=MuviMatchr`.
    - The token `ddl-auto` appears on exactly one non-comment line of `src/main/resources/application.properties`, and that line reads `spring.jpa.hibernate.ddl-auto=validate`.
    - `src/main/resources/application.properties` contains `spring.datasource.url=jdbc:postgresql://localhost:5432/muvimatchr` and `spring.flyway.enabled=true`.
  </acceptance_criteria>
  <verify>
    <automated>docker compose config > /dev/null && grep -qF 'postgres:18' docker-compose.yml && grep -qF '127.0.0.1:5432:5432' docker-compose.yml && echo COMPOSE_OK</automated>
    <fails_when>non-zero exit, `services.db` validation errors in the output, or `COMPOSE_OK` absent — meaning the compose file is malformed, uses a different image tag than the Testcontainers fixture, or publishes the port on all interfaces</fails_when>
    <automated>docker compose up -d --wait && docker compose ps --format '{{.Service}} {{.Health}}' | grep -q 'db healthy'</automated>
    <fails_when>non-zero exit, or `db healthy` absent from the output — `--wait` timed out because the container never passed its `pg_isready` healthcheck</fails_when>
    <automated>test "$(grep -v '^#' src/main/resources/application.properties | grep -c 'ddl-auto')" = "1" && grep -q 'spring\.jpa\.hibernate\.ddl-auto=validate' src/main/resources/application.properties && grep -q 'spring\.application\.name=MuviMatchr' src/main/resources/application.properties</automated>
    <fails_when>non-zero exit — either the property is absent, or it appears on more than one non-comment line (so which value wins is ambiguous), or it is set to something other than `validate`, or the pre-existing application-name line was clobbered</fails_when>
    <human-check>Confirm you are content to leave a PostgreSQL 18 container running locally: run `docker compose ps` and check the `db` service is the only container this project started, and that its resource footprint is acceptable on this machine. If you chose Colima in Task 1, also note that you will need `colima start` after a reboot before any integration test in this phase can run.</human-check>
  </verify>
  <done>`docker compose up -d --wait` brings up a healthy PostgreSQL 18 service bound to loopback with a correctly-mounted named volume, and application.properties carries matching datasource credentials plus `spring.jpa.hibernate.ddl-auto=validate` on exactly one line.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maven Central → build classpath | Third-party artifacts are downloaded and executed as part of every build and test run. This phase adds seven new coordinates and bumps two plugins across a major version. |
| Host network → local PostgreSQL container | The compose service listens on a TCP port with fixed, well-known credentials. Anything that can reach that port can read and write every row. |
| Developer machine → container runtime | A new privileged daemon and Linux VM are installed and left running. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | Maven/Gradle artifact resolution for the seven new coordinates plus the Boot and Kotlin plugin bumps | high | mitigate | RESEARCH.md `## Package Legitimacy Audit` verified all eight artifacts directly against repo1.maven.org's own file listings and, for Spring-managed artifacts, against the downloaded `spring-boot-dependencies-4.1.1.pom` content — a stronger check than registry heuristics because it confirms the exact artifact+version Boot 4.1.1 itself resolves. All eight verdicts are OK/Approved; zero `[ASSUMED]`, `[SUS]` or `[SLOP]`, so no legitimacy checkpoint is required. Task 2 pins every coordinate through the BOM rather than a hand-written version, and the acceptance criteria assert the exact artifact ids — which is also what catches the Testcontainers 1.x-vs-2.x artifact-id trap that would otherwise pull a second, unmanaged Testcontainers major version onto the classpath. |
| T-01-01 | Information Disclosure | `docker-compose.yml` PostgreSQL service | medium | mitigate | Publish the port as `127.0.0.1:5432:5432` so the service is reachable only from this host. The bare `5432:5432` form binds all interfaces, exposing a database whose username and password are both the literal project name to any peer on the current network. Credentials themselves stay plaintext and are accepted as such: RESEARCH.md's Runtime State Inventory confirms no production secret exists in this project yet, and these values are local-development-only. Revisit when a real deployment credential first appears (a later phase). |
</threat_model>

## Flagged Assumptions

These are recorded, not resolved. They are carried forward so a later reader knows what was assumed rather than proven.

1. **RELI-01 was not independently re-decomposed by an automated edge probe.** The deterministic edge-probe run over RELI-01's requirement text returned exactly one row, classified `unclassified` with status `unresolved` (`coverage: { applicable: 1, resolved: 0, unresolved: 1 }`), so it contributed no acceptance criteria of its own. This plan set instead treats ROADMAP.md's four enumerated Phase 1 success criteria, plus 01-VALIDATION.md's Phase Requirements → Test Map, as the complete human-reviewed decomposition of RELI-01. If RELI-01 has scope beyond those four criteria, nothing in this phase will detect it.

2. **The two-context restart mechanism is composed, not copied.** RESEARCH.md Assumption A1 records that the `SpringApplicationBuilder` + shared static Testcontainers pattern used by Plan 01-02 is synthesized from two independently well-documented Spring Boot and Testcontainers APIs, with no single source demonstrating that exact combination end-to-end. Confidence in each API individually is HIGH; confidence in the composition is MEDIUM. The first execution of `RestartSurvivalTest` is what validates it, and Plan 01-02 names the documented fallback mechanisms if it does not hold.

3. **Docker's absence was re-verified, closing RESEARCH.md Assumption A2.** `command -v` for `docker`, `colima`, `podman`, `orbstack` and `nerdctl` all returned empty and /Applications contains no container app, confirming this is a genuine absence rather than a PATH misconfiguration. Homebrew is present, so the install is automatable.

## Artifacts this phase produces

This plan (01-01) produces:

| Artifact | Kind | Note |
|----------|------|------|
| `build.gradle.kts` | modified build config | Plugins → `kotlin("jvm")`/`kotlin("plugin.spring")`/`kotlin("plugin.jpa")` 2.3.20, `org.springframework.boot` 4.1.1, `io.spring.dependency-management` 1.1.7 |
| `gradle/wrapper/gradle-wrapper.properties` | modified build config | `distributionUrl` → `gradle-8.14.3-bin.zip` |
| `src/main/resources/application.properties` | modified config | Adds `spring.datasource.url` / `.username` / `.password`, `spring.jpa.hibernate.ddl-auto=validate`, `spring.flyway.enabled=true` |
| `docker-compose.yml` | new file | Service `db`, image `postgres:18`, loopback-bound port, named volume `muvimatchr-pgdata` at `/var/lib/postgresql`, `pg_isready` healthcheck |
| `src/test/kotlin/org/example/muvimatchr/MuviMatchrApplicationTests.kt` | deleted | Superseded by `RestartSurvivalTest` (Plan 01-02) |

New dependency coordinates added to `build.gradle.kts` (all BOM-managed, no explicit versions): `org.springframework.boot:spring-boot-starter-data-jpa`, `org.springframework.boot:spring-boot-starter-flyway`, `org.flywaydb:flyway-database-postgresql`, `org.postgresql:postgresql`, `org.springframework.boot:spring-boot-testcontainers`, `org.testcontainers:testcontainers-postgresql`, `org.testcontainers:testcontainers-junit-jupiter`.

The full phase artifact inventory across all three plans is listed in `01-03-PLAN.md`.

<verification>
- `docker info` and `docker compose version` both exit 0.
- `./gradlew --version` reports Gradle 8.14.3.
- `./gradlew build` is green with zero test classes present.
- `./gradlew dependencies --configuration runtimeClasspath` shows the PostgreSQL driver and the Flyway PostgreSQL dialect module.
- `docker compose up -d --wait` yields a healthy `db` service.
- `application.properties` sets `spring.jpa.hibernate.ddl-auto=validate` on exactly one non-comment line.
</verification>

<success_criteria>
The repository compiles and builds on the Spring Boot 4.1.1 / Kotlin 2.3.20 / Gradle 8.14.3 toolchain that D-01 locks, with the JPA, Flyway, PostgreSQL and Testcontainers dependency set resolved and no pre-existing dependency removed; a container daemon is installed and running; and D-02's local PostgreSQL 18 service is up, loopback-bound, and matched by the datasource configuration in application.properties.
</success_criteria>

<output>
Create `.planning/phases/01-persistence-foundation/01-01-SUMMARY.md` when done. Record which container runtime the developer selected, any JSpecify nullability fixes the Kotlin 2.3.20 upgrade required, and the deletion of the superseded placeholder test.
</output>
